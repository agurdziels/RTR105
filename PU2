#include<stdio.h>
int main()
{
        printf("Ievadi decimālo skaitli\n");
        char skaitlis;
        scanf("%d", & skaitlis);
        for(int i=7; i >= 0; --i)
                printf("%d", (skaitlis >> i) & 1 ? 1 : 0 );
        printf("\n");
        return 0;
}
